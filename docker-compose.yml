services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "${PORT:-3004}:3000"
    environment:
      - AI_PROVIDER=${AI_PROVIDER:-mini}
      - LOCAL_AI_BASE_URL=http://ollama-shared:11434/v1
      - MINI_MODEL=${MINI_MODEL:-phi3:mini}
      - OLLAMA_MODEL=${OLLAMA_MODEL:-gemma2:2b}
    labels:
      - traefik.enable=true
      - traefik.http.routers.obstetrascroll.rule=Host(`obstetrascroll.159.223.26.67.sslip.io`) || Host(`obstetrascroll.159.223.26.67.nip.io`)
      - traefik.http.routers.obstetrascroll.entrypoints=http
      - traefik.http.routers.obstetrascroll-https.rule=Host(`obstetrascroll.159.223.26.67.sslip.io`) || Host(`obstetrascroll.159.223.26.67.nip.io`)
      - traefik.http.routers.obstetrascroll-https.entrypoints=https
      - traefik.http.routers.obstetrascroll-https.tls=true
      - traefik.http.routers.obstetrascroll-https.tls.certresolver=letsencrypt
      - traefik.http.services.obstetrascroll.loadbalancer.server.port=3000
    restart: unless-stopped
    networks:
      - coolify

networks:
  coolify:
    external: true
